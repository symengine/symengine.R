
# Links against the downloaded binaries
PKG_CPPFLAGS= -I../windows/usr/include
PKG_LIBS= -L../windows/usr/lib -lsymengine -lmpc -lmpfr -lgmp_static

all: clean winlibs

clean:
	rm -f $(OBJECTS) $(SHLIB) 

winlibs:
	"${R_HOME}/bin${R_ARCH_BIN}/Rscript.exe" "../tools/winlibs.R"
	