
CXX_STD = CXX11

PKG_LIBS = -L../cmakebuild${WIN}/symengine -lsymengine -lmpfr -lgmp

PKG_CPPFLAGS = -I../src/upstream -I../cmakebuild${WIN}

.PHONY: all do_upstream

all: $(SHLIB)

$(OBJECTS): do_upstream

do_upstream:
	@(cd .. && ./tools/wrapped_configure.win && \
	cd cmakebuild${WIN} && $(MAKE) && $(MAKE) install)
