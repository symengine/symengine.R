# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: r
cache: packages
r:
  - release
os:
  - linux
  - osx
sudo: false

env:
  matrix:
    - SYMENGINE_COMMIT=master
    - SYMENGINE_COMMIT=4733e429883aad551138a607f2a5a40f73920f02

## FIXME: warnings should be errors when the package is stable
warnings_are_errors: false

notifications:
  email: false

## Use bin/install_travis.sh to install symengine C++ library
before_install:
- bash tools/setup_travis.sh
- export PATH="$HOME/conda_root/bin:$PATH"
- source activate $HOME/our_usr

